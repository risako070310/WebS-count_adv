<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/count.css">
  </head>
  <body>
    
    <form action="/new" method="post">
      <input type="submit" value="NEW">
    </form>
    
    <% @counters.each do |counter|%>
      <div class="counter">
        <h1 style="
          <% if counter.number % 3 == 0 || counter.number.to_s.include?("3")%>
            border: 1px solid #f8b516;
          <% elsif counter.number == 0 %>
            border: 1px solid #d8d8d8;
          <% else %>
            border: 1px solid #d8d8d8;
          <% end %>
        ">
          <%= counter.number %>
        </h1>
        <form action="/plus/<%= counter.id %>" method="post">
          <input type="submit" value="+">
        </form>
        <form action="/minus/<%= counter.id %>" method="post">
          <input type="submit" value="-">
        </form>
        <form action="/clear/<%= counter.id %>" method="post">
          <input type="submit" value="CLEAR">
        </form>
      </div>
    <% end %>
  </body>
</html>
